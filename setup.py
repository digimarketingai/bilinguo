from setuptools import setup

setup(
    name="bilinguo",
    version="1.0.0",
    py_modules=["bilinguo"],
    install_requires=["gradio", "pandas", "gtts"],
    author="digimarketingai",
    description="Build bilingual termbases with audio in minutes"
)
